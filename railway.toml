[build]
builder = "RAILPACK"

[build.env]
MISE_PYTHON_GITHUB_ATTESTATIONS = "false"

[deploy]
startCommand = "bash start.sh"
healthcheckPath = "/api/bots"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
