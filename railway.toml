[build]
builder = "RAILPACK"

[build.env]
MISE_PYTHON_GITHUB_ATTESTATIONS = "false"
PYTHONUNBUFFERED = "1"

[deploy]
startCommand = "python -u -m backend"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
