[build]
builder = "RAILPACK"

[build.env]
MISE_PYTHON_GITHUB_ATTESTATIONS = "false"

[deploy]
startCommand = "python3 -m backend"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
